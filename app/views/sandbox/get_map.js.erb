var listingSideBar = $('#hidden_map');
if (listingSideBar.length > 0) {
	console.log("listing:");
}
console.log("<%= @pov['pano_id'] %>" + "<%= @pov['heading'] %>");
console.log("<%=  @listing['oohm_id']  %>" + "<%=  @listing['oohm_vendor']  %>");




// if (navigator.geolocation) {
//   navigator.geolocation.getCurrentPosition();
// } else {
//   error('Geo Location is not supported');
// }


// navigator.geolocation.getCurrentPosition(function(position) {
//   do_something(position.coords.latitude, position.coords.longitude);
// });


	// var APP = {};

// APP.mappy = new google.maps.Map(document.getElementById("hidden_map"),
//     mapOptions);   

